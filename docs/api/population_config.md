
# population_config Module

Population configuration and initialization.

## Overview

The `natal.frontend.model.draft` module (`ModelDraft`) handles population configuration, including survival schedules, fecundity rates, and initial population setup; the assembly entry points live in `natal.frontend.model.assembly`.

## Complete Module Reference

::: natal.frontend.model.draft
    options:
      heading_level: 3