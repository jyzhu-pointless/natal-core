# 空间构建器（已并入 SpatialConfigurator）

本页原为 `SpatialBuilder` 时代的构建说明，内容与 [SpatialConfigurator：空间种群批量构造](spatial_configurator.md) 重复。旧的 Builder 类已移除，当前唯一入口是 `SpatialPopulation.builder(...)` 返回的 `SpatialConfigurator`。

- 当前构建方式、批量参数与两条构建路径：[SpatialConfigurator：空间种群批量构造](spatial_configurator.md)
- 从旧 Builder 迁移：[从 Builder 迁移到 Configurator](migration_builder_to_configurator.md)
- 运行期 deme 修改与所有权：[空间模拟](3_spatial_simulation.md)

保留本页仅为不破坏既有链接。
