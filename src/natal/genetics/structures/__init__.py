"""Structures subpackage — genetic architecture blueprint classes.

Provides the structural types (Species, Chromosome, Locus) that define the
genetic architecture of a species, along with support infrastructure for
construction (:mod:`._construction`), enumeration (:mod:`._enumeration`),
mapping (:mod:`._mapping`), pattern matching (:mod:`._pattern`), and
entity/structure registries (:mod:`._registry`).
"""
