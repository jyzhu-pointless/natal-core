# 质量检查规范

> [English version](./quality_checks_spec.md)。中英文同步；含义冲突时以英文版为准。

## 范围与职责

本规范统一管理 Python 和 Rust 代码变更（包括可执行示例）的质量政策。[AGENTS.md](./AGENTS.md) 定义授权、风险分类和审查者安排；[docstring_spec.md](./docstring_spec.md) 定义 docstring 和类型标注格式。Skill 引用这些政策，不另设阈值。

不影响运行行为或示例代码的正文、注释和排版修改，检查准确性、中英同步、链接及相关格式，不运行代码全量门禁。显著改变规则或 skill 的修改，还须按 AGENTS.md 进行独立场景推演。规范中不调用项目代码、不描述项目 API、也不表达科学模型的独立语法或风格示意，进行文档检查和片段验证，不运行项目全量门禁；项目 API 或模型的可执行示例仍按代码修改处理。

## 验证时机与命令

开发过程中运行能回答当前问题的针对性检查。代码变更交付前，对完成后的产物运行四项完整门禁：

```bash
pytest
pyright
ruff check src demos
python scripts/check_rust.py
```

仅修改 Rust 代码也适用。Rust 脚本中的 `cargo fmt --check`、`cargo clippy -- -D warnings` 和 `cargo check --all-targets` 均为硬门禁；可选的 rust-analyzer 诊断不阻断交付。使用仓库环境和配置；命令不可用时报告缺失条件，不假定成功。

主 agent 可运行检查。高风险修改由 evaluator 独立执行最终完整门禁，作者无须提前重复同一次全量检查。后续实质修改须重跑受影响检查并重新审查；若使之前全量结果失效或影响不明确，重跑完整门禁。

仅在有相关 lint 问题时运行 `ruff check src demos --fix`，并检查其 diff 中是否混入无关修改。自动修复不是无条件门禁。

公开签名、默认值、导出路径或其他影响 stub 的变更，须在最终类型检查前运行 `python scripts/generate_init_pyi.py`。必要的文档及示例更新也须在最终审查前完成。

执行受影响的 demo 和可运行文档示例，并提供所需初始化环境。大范围 API 迁移、影响示例执行的共享基础设施修改或发布验证，执行全部 demo 和可运行示例。区分说明性片段和可运行示例；报告缺失条件及未执行的相关示例。

## 测试与适用条件

优先采用 `tests/test_*.py` 中由 pytest 收集的行为测试、小型共享 fixture，以及聚焦同一行为的断言组。每个测试必须验证明确的行为合同或数学性质，并指出能发现的具体错误。已有测试能够证明覆盖本次变化时即可采用，不为凑数量增加重复测试。

覆盖变更行为、相关边界和修复问题的回归场景。其他测试按触发条件选择：

| 类别 | 必须覆盖的条件 | 验证证据 |
|---|---|---|
| 负向合同 | 本次删除或禁止接口 | 断言接口不可访问，包括受影响的导出入口 |
| 所有权 | API 承诺副本、只读结果或其他隔离保证 | 尝试修改，并验证承诺的隔离 |
| 状态转换 | 本次涉及或影响生命周期行为 | 执行相关的 restore→run、finish→snapshot、import→run、clear→record 等序列 |
| 配置组合 | 多个配置选项共同影响本次行为 | 列明有效组合及相互影响，验证关键组合 |
| 错误路径 | 输入约束或失败处理改变 | 检查异常及合同规定的失败后状态 |

不适用的类别简短说明原因，不制造无关测试。所有权以 API 合同为准：明确共享的可变视图不自动构成缺陷，只读标记本身也不能证明隔离。错误路径仅在合同承诺失败操作不产生部分修改时要求状态不变，否则验证规定的恢复行为。

小规模关键组合应穷举。组合很多时，说明代表组合、边界和生成式测试的选择依据；抽样后不得宣称已穷举。

接口不存在、异常类型、对象身份、可变性和状态等非数值断言均有效。数值测试须采用有独立依据的预期值、误差界限或数学不变量。整数结果应精确比较，浮点容差须有数值依据。仅运行不崩溃不能验证数值正确性。

随机输出的统计结论须使用有依据的样本量、容差及统计验证。说明验证的量（均值、方差或分布）、前提、可复现的种子策略以及如何控制偶发失败。不统一规定最低运行次数。确定性回归或单样本边界测试可验证特定性质，但不能据此证明分布正确。方法参见 `numerical-verification`。

## 覆盖率

- 新模块须达到至少 95% 行覆盖率。
- 已有模块新增的可执行代码须达到至少 95% 行覆盖率。分母为本次新增的全部可执行代码行数，分子为其中已执行的行数；不能用整个模块的平均值替代。
- 报告覆盖率命令、新增行测量所用的基线或 diff、测量范围和未覆盖行。例如 `pytest --cov=src/natal --cov-report=term-missing --cov-report=json` 提供 Python 行数据；已有模块还须与变更行对应。
- Rust 可执行代码变更使用适合该语言的覆盖率工具；Python 覆盖率不能证明 Rust 覆盖率。
- 无法可靠测量时标记“未确认”，必需测量视为受阻，不得估算后宣称达标。
- 覆盖率不能替代合同或数值检查，不得通过排除可执行行来达到阈值。

## 类型、Docstring 与 Lint

使用仓库当前的 Pyright strict 配置和 Ruff 配置，不在 skill 中复制容易过时的配置。函数参数、返回值和公开属性按 docstring 规范标注；明显的局部变量类型允许推断，推断不明确或类型表达重要约束时才补充标注。

使用精确类型。`Any` 和 `object` 用作宽泛注解时须有具体书面理由；`object` 可以合理表示使用前需要收窄的未知值。不得用宽泛类型替代精确类型来消除报错。禁止 `cast(Any, …)`。`cast(T, x)` 仅用于有依据、但静态分析无法证明的运行时不变量，优先使用类型收窄或重构。每个 `# type: ignore` 都是最后手段，须附简短具体原因。

纯类型修改须保留运行行为。收紧合同须同步测试和文档。沿用既有且合理的 lint 例外；新增抑制必须范围小且有解释。不得关闭整类规则、保留死代码或扩大忽略范围来制造通过结果。

修复范围内违反必需类型、docstring 和 lint 规范的问题仍须修复，但报告时应说明实际影响，与科学计算或运行错误区分。架构偏好和可读性建议不自动阻断交付。

## 修复范围与基线失败

修复本次修改的代码文件中的全部门禁错误，以及本次修改导致的其他文件失败。适用于 pytest、Pyright、Ruff 和 Rust 门禁。不得仅为全仓变绿而将任务扩大到无关文件。

未修改且未受影响文件的失败，只有在变更前复现、或在记录的基线版本及可比环境下复现等证据支持时，才可认定为既有问题。熟悉的报错或未变化的文件名不构成证据。不得 reset 或覆盖用户修改来取得基线。

记录失败命令、诊断、基线版本或保存的状态、环境对比，以及本次变更为何不影响它。已确认的无关基线失败不必阻断交付，但完整门禁仍须记录为失败并关联已接受的基线问题，不得标为通过。修改的文件和本次导致的失败不适用此例外。

来源无法确定时继续调查。已证实的范围内缺陷必须修复；缺少证据或基础设施而无法完成必需验证时，任务受阻。不得悄悄重分类失败、跳过测试、压制诊断或改变阈值。

## 审查证据与结论

记录四项内容：本次要求及证据、本次引入或波及的问题、已确认的无关基线问题，以及未完成的必需检查。

每项要求或检查标为 `PASS`、`FAIL`、`NOT CHECKED` 或 `N/A`，必要时说明原因。基线失败保留命令失败结果，并关联基线证据。

| 结论 | 条件 |
|---|---|
| `APPROVED` | 范围内无阻断问题且必需验证已完成；无关基线例外有证据并已披露 |
| `REJECTED` | 存在范围内缺陷、必需规范违规、缺失适用测试，或实测覆盖率低于 95%，需要修复 |
| `BLOCKED` | 缺少工具、环境、证据或独立审查导致必需验证无法完成，且尚无已确定的拒绝理由 |

既有确定缺陷又有未完成检查时，返回 `REJECTED`，同时列出受阻检查。未检查项不能视为通过。批准仅针对审查过的产物，不表示已接受的基线失败已经消失。

优先修正确性及失败测试，再处理合同和类型问题，最后处理格式及 lint。交付报告包含变更文件、行为和原因、实际执行的命令与结果、适用时的审查者身份，以及剩余限制。
